# ---- Core framework -------------------------------------------------------
Django==5.1.5
djangorestframework==3.15.2
django-environ==0.11.2
# 3.2.13+ is the first release with binary wheels for Python 3.13/3.14.
psycopg[binary]==3.2.13

# ---- Auth / security ------------------------------------------------------
djangorestframework-simplejwt==5.3.1
django-axes==7.0.0                 # account lockout / brute-force protection
django-otp==1.5.4                  # MFA (TOTP)
qrcode==8.0                        # MFA enrolment QR codes
argon2-cffi==23.1.0                # password hashing (stronger than PBKDF2)

# ---- API surface ----------------------------------------------------------
django-filter==24.3
drf-spectacular==0.27.2            # OpenAPI 3 schema + Swagger UI
django-cors-headers==4.6.0

# ---- Async / scheduling ---------------------------------------------------
celery==5.4.0
redis==5.2.1
django-celery-beat==2.7.0
django-redis==5.4.0

# ---- Documents / reporting ------------------------------------------------
# HTML -> PDF. WeasyPrint supports real CSS (Paged Media, flexbox, @page
# margin boxes), so the invoice and report templates are ordinary stylesheets
# rather than nested tables with hard-coded point widths.
#
# It links against native Pango/Cairo libraries. The Dockerfile installs them;
# on a Windows dev box they come from the GTK runtime, which must be installed
# separately or PDF rendering fails with an OSError at import time.
weasyprint==63.1
openpyxl==3.1.5                    # Excel export

# ---- External services ----------------------------------------------------
requests==2.32.3                   # OBR EBMS declaration transport
# Fallback translation engine for the reader-triggered "Translate" control.
# Was installed in dev but undeclared, so deployed environments silently lost
# the fallback and every translation returned the untranslated source.
deep-translator==1.11.4

# ---- Ops ------------------------------------------------------------------
gunicorn==23.0.0
whitenoise==6.8.2
sentry-sdk==2.19.2
structlog==24.4.0

# ---- Testing --------------------------------------------------------------
pytest==8.3.4
pytest-django==4.9.0
pytest-cov==6.0.0
factory-boy==3.3.1
freezegun==1.5.1
